import React from "react";
import { StyleSheet } from "react-native";

const StyledModalButton = StyleSheet.create({
        buttonSalvar: {
        width: 100,
        height: 40,
        textAlign: 'center',
        alignItems: 'center',
        marginTop: 20,
        borderRadius: 30

    },
});

export default StyledModalButton;
